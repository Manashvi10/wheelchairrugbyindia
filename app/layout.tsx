import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "WRFI - Wheelchair Rugby Federation of India",
  description:
    "Wheelchair Rugby Federation of India (WRFI) promotes wheelchair rugby and inclusive sports across India. Empowering athletes, breaking barriers, championing inclusion.",
  keywords: [
    "wheelchair rugby",
    "India",
    "WRFI",
    "inclusive sports",
    "para sports",
    "disability sports",
  ],
  icons: {
    icon: "/icon.png",
    shortcut: "/icon.png",
    apple: "/icon.png",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
